# -*- coding: UTF-8 -*-
"""Map skill — 地图标注与路线规划领域知识。"""

MAP_SKILL = """## 地图与位置能力

你可以通过以下工具帮助用户处理地理位置相关任务：

### 地理编码（地名 → 坐标）
使用 `geocode` 工具将地名转换为经纬度。例如：
- geocode(address="广州塔") → 返回 {lng: 113.324, lat: 23.106, ...}
- 可指定 city 参数提高准确度：geocode(address="望京", city="北京")

### 逆地理编码（坐标 → 地址）
使用 `regeocode` 工具根据经纬度查询地址信息。

### 地图显示（```map 代码块）
当你获取到地点坐标后，用 ```map 代码块在聊天中嵌入地图：
- **title**: 地图标题
- **center**: 地图中心点 [lng, lat]
- **zoom**: 缩放级别（3=全国, 10=城市, 14=街区, 18=建筑）
- **markers**: 标记点数组，每个含 lat/lng/title/desc

### 路线规划（```route 代码块）
当用户询问两地之间的路线时：
1. 先用 `geocode` 查询起点和终点的坐标
2. 再用 ```route 代码块输出路线：
   - **mode**: driving(驾车) / walking(步行) / riding(骑行) / transit(公交)
   - **from/to**: 起终点对象，含 lng/lat/name

### 常见交互模式
- "广州在哪里？" → geocode → ```map 显示
- "从A到B怎么走？" → geocode A + geocode B → ```route 显示
- "这附近有什么？" → regeocode → 返回周边信息

### 坐标参考
- 中国范围大致: lng 73~135, lat 18~54
- 北京: [116.40, 39.90]
- 上海: [121.47, 31.23]
- 广州: [113.25, 23.13]
- 深圳: [114.06, 22.54]
"""
